package scraper

func crawl(seedURL string, maxPages int) {
	// create queue
	// create set
	// enqueue seed URL
	// loop while queue is not empty AND pages visited is under max
	// dequeue next URL
	// if already visited, skip
	// mark as visited
	// fetch page
	// if fetch failed, skip
	// tokenize page, get list of URLs
	// enqueue each new URL
	// increment page count
}